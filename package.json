{
  "name": "hemisphere",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal browser extension plus lightweight cloud service that demotes US-topic content.",
  "author": "Quentin Casares",
  "license": "UNLICENSED",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node scripts/build.js",
    "build:watch": "node scripts/build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "jsdom": "^29.1.1",
    "vitest": "^2.0.0"
  }
}
