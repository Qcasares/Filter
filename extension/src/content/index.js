// Content entry point. Phase 2 wires the scanner and the demote UI in here.
// Kept minimal in Phase 1 so the extension loads cleanly with a working popup.
console.debug('[Hemisphere] content script ready');
